let myFind = function(arrray, callback){
  //loop through every element in the array

  //for every element, execute the callback and store the result
  
  //if the result is "true" then you found an eleemnt
  //that meets the criteria, so return the element

  //if you go through the entire loop, and have not returned
  //(ie. you are still in the fucntion)
  //that means no element met the criteria, so return undefined (or do nothing)
}